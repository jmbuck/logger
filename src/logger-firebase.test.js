import "./database/Auth";
import "./database/Database";
import rewire from "rewire";

describe("Logger Firebase Integration", () => {
	beforeAll(() => {

	});

	it("should parse milliseconds to hours, minutes, seconds", () => {
		//const component = Enzyme.shallow(<App />);


	});
});

